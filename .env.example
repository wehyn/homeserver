DATABASE_PATH=./data/nimbus.db
# Host Docker socket path; use with docker-compose.docker.yml only.
DOCKER_SOCKET=
# Optional bearer token for Docker discovery requests.
DOCKER_AGENT_TOKEN=
# Optional bearer token shared by Nimbus and the Compose metrics agent.
MEMORY_AGENT_TOKEN=
